services:
  db:
    image: mariadb:latest
    environment:
      MARIADB_ROOT_PASSWORD: ${ROOT_PASSWORD}
    restart: always

  web:
    build: .
    ports:
      - "8058:80"
    environment:
      ROOT_PASSWORD: ${ROOT_PASSWORD}
    depends_on:
      - db
